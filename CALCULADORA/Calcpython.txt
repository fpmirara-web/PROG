def calculadora():
    print("Escolha a operação:")
    print("1 - Adição")
    print("2 - Subtração")
    print("3 - Multiplicação")
    print("4 - Divisão")
    escolha = input("Digite o número da operação (1/2/3/4): ")
    num = float(input("Digite o primeiro número: "))
    num2 = float(input("Digite o segundo número: "))

    if escolha == '1':
        resultado = num + num2
        print(f"Resultado: {resultado}")
    elif escolha == '2':
        resultado = num - num2
        print(f"Resultado: {resultado}")
    elif escolha == '3':
        resultado = num * num2
        print(f"Resultado: {resultado}")
    elif escolha == '4':
        if num2 != 0:
            resultado = num / num2
            print(f"Resultado: {resultado}")
        else:
            print("Erro: Divisão por zero não é permitida.")
    else:
        print("Opção inválida. Por favor, escolha uma operação válida.")

# Para executar a calculadora, descomente a linha abaixo:
# calculadora()
# Este é um exemplo de uma calculadora simples em Python.
# Você pode descomentar a chamada da função para testá-la.
# A função calculadora permite ao usuário escolher uma operação matemática
def main():
    while True:
        calculadora()
        continuar = input("Deseja realizar outra operação? (s/n): ")
        if continuar.lower() != 's':
            breakcalcular(num1, num2, operação):
            if operação =='+':
                return num1 + num2  
            elif operação == '-':
                return num1 - num2  
            elif operação == '*':
                return num1 * num2
            elif operação == '/':
                if num2 != 0:
                    return num1 / num2
                else:
                    return "Erro: Divisão por zero não é permitida."    
                else:
return "Operação inválida. Por favor, escolha uma operação válida."

while True:
    print(\nDigite a operação (+, -, *, /) ou 'sair' para encerrar: "   )
          operção=/ input(Operação:")
                          if operação.lower() == 'sair':
                          print("Encerrando a calculadora.")
                          break
                          
                          try:
                          num1 = float(input("Digite o primeiro numero: "))
                          num2 = float(input("digite o segundo numero: "))
                          resultado = calcular(num1, num2, operação)
                          except ValueError:
                          print ("Entrada inválida. Por favor, digite números válidos.")
        print("Obrigado por usar a calculadora.")
        